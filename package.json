{
  "name": "hk-company-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for HK company search",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
